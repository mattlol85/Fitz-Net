pipeline {
    agent {
        label "raspberrypi"
    }
    stages {
        stage('Checkout') {
            steps {
                sh 'ls -la'
                checkout([$class: 'GitSCM', branches: [[name: 'dev/panelRpiAPI']], doGenerateSubmoduleConfigurations: false, extensions: [], submoduleCfg: [], userRemoteConfigs: [[url: 'https://github.com/mattlol85/Fitz-Net.git']]])
                sh 'ls -la'
            }
        }
        stage('Build') {
            steps {
                sh 'ls -la'
                sh 'mvn clean package'
                sh 'ls -la'
            }
        }
    }
}
